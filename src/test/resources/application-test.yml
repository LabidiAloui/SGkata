spring:
  jpa:
    database: H2
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    datasource:
      url: jdbc:h2:mem:templatedb;DB_CLOSE_ON_EXIT=FALSE
      password: template
      driverClassName: org.h2.Driver
    application:
    name: kata